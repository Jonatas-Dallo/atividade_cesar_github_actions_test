{
  "name": "atividade-cesar-frontend",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:list": "node --test --test-only --test-reporter=spec tests/*.test.js"
  }
}